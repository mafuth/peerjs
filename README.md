# peerjs (web3)
simple peer to peer text message and video call app with end to end encryption
using node js

Run server: 
    ``` sh
    $ npm install
    ```
    
    ``` sh
    $ node server/bin.js
    ```